# 股票监测工具 PRD

**文档版本：** v1.0
**编写日期：** 2026-04-02
**作者：** 产品团队

---

## 一、项目背景与目标

### 1.1 项目背景

投资者在交易日需要实时关注市场整体走势及自选股的波动情况，但人工盯盘效率低且容易遗漏关键异动。本工具旨在通过程序化手段，自动监测上证指数及自选股的异常波动，在触发预设条件时第一时间通过飞书机器人推送告警通知，帮助用户快速响应市场变化。

### 1.2 项目目标

- 实时监测上证指数与指定股票的波动情况
- 在触发异动条件时，通过飞书机器人及时推送通知
- 提供轻量、低资源占用的解决方案，可长期运行
- 支持灵活配置股票列表和监控参数

---

## 二、功能需求

### 2.1 功能需求列表

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|--------|
| **F1 上证指数监测** | 监测上证指数（SH000001）变动，变动幅度超过 ±1% 时标记为异动 | **P0** |
| **F2 个股波动监测** | 监测配置文件中的个股，当日价格变动幅度超过 ±3% 时标记为异动 | **P0** |
| **F3 飞书消息通知** | 当同时满足「上证异动 + 个股异动 + 交易时间段」三个条件时，向飞书 webhook 发送告警消息 | **P0** |
| **F4 开盘异动监测** | 监测个股开盘价与昨日收盘价的差值，差值达到 ±2%（默认阈值）时触发告警，单独发送通知 | **P1** |
| **F5 配置文件管理** | 通过 TOML/JSON 配置文件管理股票列表（支持增删改查，无需代码变更） | **P1** |
| **F6 监控周期配置** | 监控轮询间隔可通过系统配置，默认 5 分钟 | **P1** |
| **F7 触发记录持久化** | 所有触发记录（含时间、股票、变动幅度）写入本地文件，保留 7 天后自动清理 | **P2** |
| **F8 去重过滤** | 同一只股票在同一监控周期内相同方向异动仅告警一次，避免重复通知 | **P2** |

### 2.2 功能详情

#### F1 上证指数监测

- 指数代码：`SH000001`（上证指数）
- 变动阈值：±1%（相对于前一日收盘价）
- 数据来源：使用新浪/腾讯等免费股票 API

#### F2 个股波动监测

- 默认阈值：±3%（相对于前一日收盘价）
- 初始股票列表：

| 股票名称 | 股票代码 | 市场 |
|---------|---------|------|
| 五洲新春 | 603667 | 上交所 |
| 三花智控 | 002050 | 深交所 |
| 卧龙电驱 | 600580 | 上交所 |
| 中大力德 | 002896 | 深交所 |

#### F3 飞书消息通知

- Webhook 地址：`https://open.feishu.cn/open-apis/bot/v2/hook/f422ffbd-8754-4bba-b992-2e25abc14750`
- 消息格式：Markdown 卡片，包含股票名称、代码、当前价、涨跌幅、异动时间
- 发送失败时记录日志，不阻塞主流程

#### F4 开盘异动监测

- 触发条件：个股开盘价与昨日收盘价差值达到 ±2%（可配置）
- 通知时机：每个监控周期内，开盘后首次检测到异动即告警
- 与 F3 独立：开盘异动属于独立事件，不要求上证指数同时异动

#### F5 配置文件管理

- 配置文件格式：`config.toml`
- 配置项包括：
  - `stock_list`：股票代码列表
  - `index_code`：上证指数代码（默认 SH000001）
  - `index_threshold`：上证指数阈值（默认 1.0）
  - `stock_threshold`：个股阈值（默认 3.0）
  - `open_threshold`：开盘异动阈值（默认 2.0）
  - `poll_interval`：监控周期（默认 300 秒）
  - `feishu_webhook`：飞书 Webhook 地址
  - `retention_days`：记录保留天数（默认 7）

#### F6 监控周期配置

- 默认轮询间隔：5 分钟（300 秒）
- 允许范围：60 秒 ~ 3600 秒
- 交易时间段自动跳过：
  - 非交易时间不发送个股告警（F3 条件之一）
  - 开盘异动监测（F4）仅在 09:30 前有效

#### F7 触发记录持久化

- 存储位置：`logs/alerts.json`
- 记录字段：`timestamp`、`stock_code`、`stock_name`、`price`、`change_percent`、`alert_type`
- 自动清理：每启动时清理超过 7 天的记录

#### F8 去重过滤

- 同一股票同一方向（涨/跌）在当前交易日内仅告警一次
- 标记已告警状态，日终（15:00 后）重置

---

## 三、非功能需求

### 3.1 性能需求

- **内存占用**：峰值不超过 50MB
- **CPU 占用**：轮询期间 CPU 使用率 < 1%
- **启动时间**：冷启动完成时间 < 3 秒
- **网络请求**：每次轮询 HTTP 请求数尽量合并，超时时间 10 秒

### 3.2 可用性需求

- 程序异常退出后自动重启（依赖 systemd 或 supervisor）
- 飞书通知失败不影响主监控流程
- 网络中断时跳过本次轮询，日志记录后继续下次轮询

### 3.3 安全需求

- Webhook 地址不硬编码，存储于配置文件
- 日志文件不包含敏感信息（不含账户、密码等）
- 配置文件权限设置为 600（仅所有者可读写）

### 3.4 兼容性需求

- 目标平台：Linux（x86_64）
- 依赖 Rust 1.70+
- 无需外部运行时（静态编译）

---

## 四、用户流程描述

### 4.1 首次部署流程

1. 下载或编译二进制文件
2. 修改 `config.toml`，确认股票列表和 Webhook 地址
3. 将二进制文件和配置文件部署到目标机器
4. 配置 systemd 服务实现开机自启
5. 启动服务，观察日志确认运行正常

### 4.2 日常使用流程

1. 服务后台运行，自动按配置的周期轮询
2. 触发异动条件 → 飞书机器人推送消息
3. 用户点击消息链接，查看相关行情
4. 无需人工干预，持续自动运行

### 4.3 配置变更流程

1. 停止服务（或编辑后 reload 配置）
2. 修改 `config.toml` 中的股票列表或阈值
3. 重启服务
4. 确认新配置生效

### 4.4 异常处理流程

1. 飞书通知发送失败 → 记录日志，不重试，不阻塞
2. API 请求超时 → 跳过本次该股票，继续轮询其他股票
3. 所有 API 不可用 → 日志记录，等待下次轮询
4. 程序崩溃 → systemd 自动重启

---

## 五、验收标准

### 5.1 功能验收

| 验收项 | 验收条件 |
|-------|---------|
| 上证监测 | 运行期间上证指数涨跌超过 ±1%，日志中应出现对应异动记录 |
| 个股监测 | 指定个股涨跌超过 ±3%，日志中应出现对应异动记录 |
| 飞书通知 | 同时触发「上证异动 + 个股异动 + 交易时间」时，飞书群应收到机器人消息 |
| 开盘异动 | 个股开盘价与昨日收盘价差值达到 ±2%，应收到独立飞书通知 |
| 配置生效 | 修改 `config.toml` 后重启服务，新配置应立即生效 |
| 记录保留 | `logs/alerts.json` 应记录所有告警，7 天后自动清理 |
| 去重有效 | 同一股票同一方向日内异动仅收到一次通知 |

### 5.2 非功能验收

| 验收项 | 验收条件 |
|-------|---------|
| 内存占用 | 正常运行 24 小时后，内存占用不超过 50MB |
| CPU 占用 | 轮询间隔内 CPU 占用 < 1% |
| 故障恢复 | kill 掉进程后，systemd 应在 10 秒内自动重启 |
| 低配置兼容 | 在 512MB 内存的 VPS 上可正常运行 |

### 5.3 交付物清单

1. 编译好的二进制文件（Linux x86_64）
2. 配置文件 `config.toml`（含注释说明）
3. systemd 服务单元文件 `stock-monitor.service`
4. 本 PRD 文档
5. 快速部署指南（可选）

---

## 六、风险与约束

| 风险/约束 | 说明 | 应对措施 |
|----------|------|---------|
| 第三方 API 不稳定 | 免费股票数据 API 可能限流或不可用 | 使用多个数据源备选；超时设置合理 |
| 非交易时段告警 | 停牌、数据延迟可能导致误判 | 严格在交易时间段内触发 F3 条件 |
| Webhook 失效 | 飞书机器人 Webhook 可能失效或被撤回 | 配置文件中可更新 Webhook URL |
| 服务器时区 | 服务器时区不一致可能影响交易时间段判断 | 强制使用 Asia/Shanghai 时区 |
| A股涨跌停 | 个股涨跌停时价格不再变动，但异动已触发 | 触发即告警，不做额外处理 |

---

## 七、后续迭代方向（暂不纳入本期）

- 支持邮件、企业微信等其他通知渠道
- 支持 Web 管理界面
- 历史数据可视化（K 线图、异动统计）
- 支持港股、美股监测
- 接入付费实时数据 API（提高数据可靠性）
